serviceMonitor:
  enabled: false

config:
  datasource:
    host: "the-book-boutique-server.privatelink.postgres.database.azure.com"
    user: "appuser@the-book-boutique-server"
    passwordSecret: 
      name: book-shop-postgres-secret
      key: password
    port: "5432"
    database: "bookshop"
    sslmode: require
  excludeDatabases: []

