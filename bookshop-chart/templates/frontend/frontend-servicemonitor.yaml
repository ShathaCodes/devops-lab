apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    serviceMonitorSelector: {{ .Values.frontend.service.serviceMonitor.serviceMonitorSelector }}  
  name: {{ .Values.name }}-frontend
  namespace: {{ .Values.monitoring.namespace }}
spec:
  endpoints:
  - interval: {{ .Values.frontend.service.serviceMonitor.interval }} 
    targetPort: {{ .Values.frontend.container.port }}
    path: {{ .Values.frontend.service.serviceMonitor.path }} 
  namespaceSelector:
    matchNames:
    - {{ .Values.namespace }}
  selector:
    matchLabels:
      app: {{ .Values.name }}-frontend